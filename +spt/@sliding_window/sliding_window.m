classdef sliding_window < spt.abstract_spt
    % SLIDING_WINDOW - Blind source separation in sliding windows
    
    properties 
        
       SPT;                     % The single-window transform               
       CommonInitSamples = 0;   % If >0 then use common initialization
       
    end
    
    
    
end