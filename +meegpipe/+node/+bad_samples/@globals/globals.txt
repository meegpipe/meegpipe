# 
# Global variables for package pset.node.bad_samples
#

# Construction defaults for class bad_samples
MADs            10
WindowLength    @(fs) fs/2
WindowShift     @(fs, wl) ceil(wl/20)
Percentile      [25 75]
MinDuration     @(fs) 2*fs
        